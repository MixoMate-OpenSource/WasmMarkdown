{
  "name": "wasm-markdown-root",
  "version": "1.0.0",
  "description": "Root workspace for WasmMarkdown project",
  "scripts": {
    "wasm:build": "cd wasm-md-core && wasm-pack build --target web --release && mkdir -p ../wasm-md-web/src/wasm ../wasm-md-web/public/wasm && cp pkg/wasm_md_core.js ../wasm-md-web/src/wasm/ && cp pkg/wasm_md_core.d.ts ../wasm-md-web/src/wasm/ && cp pkg/wasm_md_core_bg.wasm ../wasm-md-web/public/wasm/",
    "web:install": "cd wasm-md-web && npm install",
    "web:dev": "cd wasm-md-web && npm run dev",
    "dev": "npm run wasm:build && npm run web:dev"
  }
}
